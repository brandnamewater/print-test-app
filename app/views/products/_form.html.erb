<!-- <h2>Create Product</h2>

  <%= form_for(@product), remote: true do |form| %>
    <%= form.text_field :title %>
    <%= form.text_field :product_type %>
    <%= form.text_field :vendor %>
  <%= form.submit %>
  <% end %> -->


  <%= form_with(model: product, local: true) do |form| %>
    <% if product.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

        <ul>
        <% product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>


  Using Join Table
    <%# <%= form.collection_check_boxes(:category_ids, Category.all, :id, :name, :multiple => true) do |c| %> %>
      <%# <%= c.label class:"form-check-inline" do %> %>
        <%# <%= c.check_box + c.text %> %>
      <%# <% end %> %>
    <%# <% end %> %>
  |
    <%# <%= form.collection_check_boxes(:style_ids, Style.all, :id, :name, :multiple => true) do |c| %> %>
      <%# <%= c.label class:"form-check-inline" do %> %>
        <%# <%= c.check_box + c.text %> %>
      <%# <% end %> %>
    <%# <% end %> %>


  <p> No join Table</p>
    <%= form.collection_select :category_id, @categories, :id, :name %>
  <br>
    <%= form.collection_select :style_id, @styles, :id, :name %>

  <br>

    sku<%= form.text_field :sku %>

    <%= form.select :color, [["WHITE"], ["BLACK"], ["BLUE"], ["PINK"]] %>


    <%= form.select :size, [["S"], ["M"], ["L"], ["XL"], ["2XL"]] %>
  <br>
    DTG price: <%= form.text_field :price %>
    <br>
    EMBROIDERY price: <%= form.text_field :price_embroidery %>
    <br>

    country <%= form.text_field :country %>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
